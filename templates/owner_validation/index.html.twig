{% extends 'base.html.twig' %}

{% block title %}Validate Step{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Validate this step</h1>
    
    <ul>
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
        {{ form_row(form.file) }}
        {{ form_end(form) }}
    </ul>
</div>
{% endblock %}
